import React from "react";

export const Header = () => (
    <div style={style}>
        <h4>Two Blokes Afloat</h4>
        <h6>(in a boat)</h6>
    </div>
);

const style = {
    background: 'black',
    color: 'white',
    textAlign: 'center',
    padding: 5
};
